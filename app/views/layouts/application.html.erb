<!DOCTYPE html>
<html>
<head>
  <title>Amazonia</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<header>
    <nav class="nav">
      <h1 class="col-md-2"><%= link_to 'Amazonia', root_url %></h1>
      <div class="col-md-6">
        <% if notice %>
            <p class="alert alert-success center"><%= notice %></p>
        <% end %>
        <% if alert %>
            <p class="alert alert-danger center"><%= alert %></p>
        <% end %>
      </div>
      <p class="navbar-text pull-right">
        <% if user_signed_in? %>
            <%= link_to "#{current_user.email}", edit_user_registration_path,
                        :class => 'navbar-link btn btn-primary' %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete,
                        :class => 'navbar-link btn btn-primary' %>
            <% if current_user.admin %>
                <%= link_to 'Admin Page', admin_path, :class => 'navbar-link btn btn-primary' %>
            <% end %>
        <% else %>
            <%= link_to "Sign up", new_user_registration_path,
                        :class => 'navbar-link btn btn-primary' %>
            <%= link_to "Login", new_user_session_path,
                        :class => 'navbar-link btn btn-primary' %>
        <% end %>
      </p>
    </nav>
</header>

<%= yield %>

</body>
</html>
